from math import *

def num_rotation(obj,x,y,screen):
 screen.blit(obj,(x,y))
 acceleration_two=acceleration_two
 angle = 0.
 speed = (2*3.14)/7.2
 radius = 150
 angle += speed * acceleration_two
 x = cos(angle) * radius + 420
 y = sin(angle) * radius + 280
 
